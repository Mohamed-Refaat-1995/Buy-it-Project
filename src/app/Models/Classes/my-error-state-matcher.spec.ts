import { MyErrorStateMatcher } from './my-error-state-matcher';

describe('MyErrorStateMatcher', () => {
  it('should create an instance', () => {
    expect(new MyErrorStateMatcher()).toBeTruthy();
  });
});
